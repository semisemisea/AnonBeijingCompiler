int main() {
  int x = getint();
  putint(x / 1);
  putch(10);
  putint(x % 1);
  putch(10);
  putint(x / -1);
  putch(10);
  putint(x % -1);
  putch(10);
  putint(x / 2);
  putch(10);
  putint(x % 2);
  putch(10);
  putint(x / -4);
  putch(10);
  putint(x % -4);
  putch(10);
  putint(x / 3);
  putch(10);
  putint(x % 3);
  putch(10);
  return 0;
}
